package bottomtextdanny.braincell.mod.capability.player.accessory;

public enum JumpPriority {
   INSTANT,
   LARGE;

   // $FF: synthetic method
   private static JumpPriority[] $values() {
      return new JumpPriority[]{INSTANT, LARGE};
   }
}
